{
  "name": "muteddarkmode",
  "version": "1.0.0",
  "private": true,
  "dependencies": {
    "archiver": "^7.0.1"
  },
  "scripts": {
    "build": "node scripts/build.js",
    "clean": "node scripts/cleanDist.js",
    "publish": "npm run clean && node scripts/publish.js",
    "extract": "node scripts/removeAllCSSPropsExcept.js",
    "darken": "node scripts/darkenAllColoursByPercent.js",
    "lightenText": "node scripts/lightenText.js"
  },
  "eslintConfig": {
    "extends": [
      "react-app",
      "react-app/jest"
    ]
  }
}